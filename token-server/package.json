{
  "name": "copilot-lab-token-server",
  "version": "1.0.0",
  "description": "Azure Function token server for Bot Framework Direct Line — exchanges the permanent secret for a short-lived token",
  "main": "src/functions/*.js",
  "scripts": {
    "start": "func start",
    "test": "echo \"No tests configured\" && exit 0"
  },
  "dependencies": {
    "@azure/functions": "^4.0.0"
  },
  "engines": {
    "node": ">=20.0.0"
  }
}
